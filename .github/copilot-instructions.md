# Workspace Instructions

- Use JavaScript only. Keep backend contracts immutable and append `dataVerification` to emitted metrics.
- Run `npm test` after backend changes and `npm run build` after frontend changes.
- Do not replace verified PSX, broker, TradingView, or PUCARS adapters with fabricated production data.
